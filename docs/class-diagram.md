# Class Diagram

> Full system class diagram, grouped visually by layer. Include attributes
> and main methods with UML visibility (+ public, - private, # protected),
> mark abstract classes/methods, and represent all relationships
> (inheritance, association, aggregation, composition, dependency) with
> multiplicity where applicable.

```mermaid
classDiagram
    %% TODO: model layer classes
    %% TODO: persistence layer classes
    %% TODO: service layer classes
    %% TODO: ui layer classes
    %% TODO: relationships with multiplicity
```
